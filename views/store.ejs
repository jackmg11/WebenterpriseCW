<%- include("partials/header") -%>


            

     
        
        <div style="margin-left:25%; margin-right:30%;">
            <% if (admin) { %> <a href="/store/manage"><button class="waves-effect waves-dark btn" type="button">Manage</button></a> <% } %>
            <a href="/store/mybasket"><button class="waves-effect waves-dark btn" type="button">My Basket</button></a>
            Your Basket Has <%= ammountInBasket %> Items
            <% items.forEach((item) =>{ %>
                <div class="col s12 m7">
                    <div class="card horizontal">
                      <div class="card-stacked">
                        <div class="card-content">
                          <p><%= item.name %></p>
                        </div>
                        <div class="card-action">
                            <p>£<%= item.price %></p>
                            <button class="waves-effect waves-dark btn" type="button" onclick="addItemToBasket('<%= item._id %>')">Add to basket</button>
                        </div>
                      </div>
                    </div>
                  </div>
                
                
                
            <% }) %>
            </div>
        

<script>
    function addItemToBasket(id) {
        $.post("", {"action": "add", "id": id}, function(data, status, jqXHR){
            if (data === "item added"){
                console.log("Item added")
                window.location.reload()
            }
        })
    }
    
</script>
      <!--JavaScript at end of body for optimized loading-->
   
  <%- include("partials/footer") -%>